fastapi
uvicorn[standard]
pillow
diffusers
transformers
onnxruntime
safetensors
accelerate
# onnx # onnx is often a dependency of onnxruntime, explicit add if needed.
# torch # diffusers will pull in torch
